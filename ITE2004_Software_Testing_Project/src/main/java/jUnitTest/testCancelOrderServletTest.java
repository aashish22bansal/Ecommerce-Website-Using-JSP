package jUnitTest;

import static org.junit.Assert.assertEquals;
import org.junit.Test;
import org.apache.log4j.Logger;

public class testCancelOrderServletTest {
	private Logger log = Logger.getLogger(this.getClass());
}
